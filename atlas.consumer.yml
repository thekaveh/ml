name: ml-eng-lab
project_name: ml-eng-lab
profile: dev

brand:
  name: ML Eng Lab

env:
  file: ./atlas.env.user
  values:
    BASE_PORT: auto
    JUPYTERHUB_SOURCE: container

compose_overlays:
  - ./compose/ml-eng-lab-atlas.yml
